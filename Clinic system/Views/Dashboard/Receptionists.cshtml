@model IEnumerable<User>
@{
    ViewData["Title"] = "الإستقبال";
    Layout = "_DashboardLayout";
}
<table class="table table-bordered table-hover">
    <thead>
        <tr class="bg-light">
            <th>رقم</th>
            <th>الإسم</th>
            <th>رقم الهاتف</th>
            <th>الإيميل</th>
            <th>خيارات</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() <= 0)
        {
            <tr class="bg-warning ">
                <td colspan="5">لا يوجد موظفين إستقبال لعرضهم</td>
            </tr>
        }
        else
        {
            foreach (var receptionist in Model)
            {
                <tr>
                    <td>@receptionist.UserId</td>
                    <td>@receptionist.FullName</td>
                    <td>@receptionist.PhoneNumber</td>
                    <td>@receptionist.Email</td>
                    <td class="control-options">
                        <a asp-action="UpdateUser" asp-controller="Dashboard" asp-route-id="@receptionist.UserId">تعديل البيانات</a>
                        <a asp-action="UpdateUserPassword" asp-controller="Dashboard" asp-route-id="@receptionist.UserId">تعديل الرقم السري </a>
                        <a asp-action="DeleteUser" asp-controller="Dashboard" asp-route-id="@receptionist.UserId">حذف المستخدم</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
